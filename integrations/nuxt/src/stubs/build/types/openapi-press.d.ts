// Typecheck-only stub for the generated `#build/types/openapi-press` manifest.
// The real declarations derive the name union and client types from the
// registered Press modules.

// eslint-disable-next-line @typescript-eslint/no-empty-object-type -- populated by the generated manifest
export interface PressClients {}

export type PressClientName = keyof PressClients;
